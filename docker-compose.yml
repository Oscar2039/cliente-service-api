version: '3.8'

services:
  cliente-service:
    build: .
    container_name: cliente-service-app
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=dev
    restart: unless-stopped